// was stuck and not working yet. will finish it today 


(function animation () {
    var news = document.getElementById("headlines");

    var toLeft = news.offsetLeft;
    var firstChild = document.querySelector(a)

    function newsLine() {
        news.style.left--;
        if (toleft <= headlines.firstChild.offsetWidth) {
            console.log(left);
        } else {
            news.style.left += headlines.firstChild.offsetWidth;
            headlines.appendChild(firstChild);
            headlines.remove(firstChild)
        }
        news.style.left = news.offsetLeft
        
        requestAnimationFrame(newsLine;
    }
});
animation ()