(function () {
    var news = document.getElementById("wrapper");

    var toLeft = news.offsetLeft;

    function newsLine() {
        news.style.left--;
        if (toleft < headlines.firstChild.offsetWidth) {
            console.log(left);
        requestAnimationFrame(news);
    }
});
